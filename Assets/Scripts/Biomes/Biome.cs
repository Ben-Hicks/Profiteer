using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum BiomeType { Plains, Desert, Arctic, Mountains, Wasteland, Grassland, Forest, LENGTH };

public class Biome : MonoBehaviour {

    public static readonly string[] arsBiomeNames = { "Plains", "Desert", "Arctic", "Mountains", "Wasteland", "Grassland", "Forest", "LENGTH" };

    // Start is called before the first frame update
    void Start() {

    }

    // Update is called once per frame
    void Update() {

    }
}
